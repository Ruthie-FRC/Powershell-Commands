[
  {
    "name": "Get-ChildItem",
    "category": "filesystem",
    "synopsis": "Gets items and child items in one or more locations.",
    "syntax": "Get-ChildItem [[-Path] <String[]>] [[-Filter] <String>] [-Recurse] [-Depth <UInt32>] [-Force] [-Hidden] [-File] [-Directory] [-Name]",
    "description": "Retrieves files, folders, registry keys, certificates, and other items from any PowerShell provider location. Use -Recurse to descend into subdirectories and -Filter for fast wildcard filtering at the provider level.",
    "aliases": ["gci", "ls", "dir"],
    "keywords": ["list", "files", "directory", "ls", "dir", "children", "items"],
    "examples": [
      { "description": "List files in the current directory", "code": "Get-ChildItem" },
      { "description": "List all .ps1 files recursively", "code": "Get-ChildItem -Path C:\\ -Filter *.ps1 -Recurse -ErrorAction SilentlyContinue" },
      { "description": "List only directories", "code": "Get-ChildItem -Directory" },
      { "description": "Show just names", "code": "Get-ChildItem -Name" }
    ]
  }
]
